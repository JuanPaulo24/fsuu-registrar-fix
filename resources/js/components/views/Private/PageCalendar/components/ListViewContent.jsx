import React from "react";
import { Card } from "antd";

const ListViewContent = () => {
  return (
    <Card className="p-6">
      <h2 className="text-lg font-semibold mb-2">List View (Static)</h2>
      <p>This is a placeholder for list of all scheduled services.</p>
    </Card>
  );
};

export default ListViewContent;
